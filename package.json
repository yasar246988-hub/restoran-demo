{
  "name": "restoran-demo",
  "version": "2.0.0",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "npm install --prefix backend --omit=dev && npm install --prefix frontend && npm run build:frontend",
    "build:frontend": "cd frontend && npm run build",
    "start": "node backend/server.js"
  }
}
